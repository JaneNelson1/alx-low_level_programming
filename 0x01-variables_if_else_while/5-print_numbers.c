#include <stdio.h>

/**
 * main - all codes
 * Description -Printing all single numbers
 *
 * Return: 0
 *
 */
int main(void)
{
int numbers;

for (numbers = 0; numbers < 10; numbers++)
printf("%i", numbers);
putchar('\n');
return (0);
}
